'use client'
import { memo } from 'react'

// Empty component that doesn't use Pace.js
const PaceLoader = memo(function PaceLoader() {
  // Removed Pace.js implementation
  return null
})

export default PaceLoader
