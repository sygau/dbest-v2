# Exclude API routes from Pages build (keep on Vercel)
api/
pages/api/

# Exclude development files
dev/
bustime/

# Exclude maintenance page (handled separately)
pages/maintenance.tsx

# Exclude Workers config (for later)
wrangler-workers.toml 